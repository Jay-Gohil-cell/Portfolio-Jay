const Socialmedia = (props) => {
    return (
        <img src={`${props.iconname}`} className="lg:w-10 md:w-20 w-10  hover:scale-110 transition-all duration-300" alt="" />

    )
}

export default Socialmedia
